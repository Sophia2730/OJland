<!DOCTYPE html>
<html>
  <head>
      <% include ./header.ejs %>
  </head>
  <body class="bg-dark sidenav-toggled" style="overflow:hidden">
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top" id="mainNav">
          <% include ./nav.ejs%>
      </nav>
      <div class="container">
          <div class="row">
              <div class="col-md-2"></div>
              <div class="card card-login mx-5 mt-5">
                 <div class="card-header">이메일 찾기</div>
                 <div class="card-body">
                    <div class="text-center mt-4">
                       <h4>이메일을 잊어버리셨나요?</h4>
                       <p>회원님의 정보를 입력해서 이메일을 찾을 수 있습니다!</p>
                    </div>
                    <form id="id">
                       <div class="form-group">
                          <span class="nav-link-text">이름</span>
                          <input class="form-control" name="Name" type="email" aria-describedby="emailHelp" placeholder="이름을 입력해주세요."><br>
                          <span class="nav-link-text">생년월일</span>
                          <input class="form-control" name="Birth" type="date" aria-describedby="emailHelp" placeholder="ex)19940101"><br>
                          <span class="nav-link-text">전화번호</span>
                          <input class="form-control" name="Tel" type="tel" aria-describedby="emailHelp" placeholder="ex)010-1234-5678">
                       </div>
                       <a id="getEmail" class="btn btn-primary btn-block" href="">이메일 찾기</a>
                    </form>
                 </div>
              </div>
              <div class="card card-login mx-5 mt-5">
                  <div class="card-header">비밀번호 찾기</div>
                  <div class="card-body">
                     <div class="text-center mt-4">
                        <h4>비밀번호를 잊어버리셨나요?</h4>
                        <p>가입하신 이메일로 임시 비밀번호를 보내드립니다!</p>
                     </div>
                     <form id="pwd">
                        <div class="form-group">
                          <span class="nav-link-text">이름</span>
                          <input class="form-control" name="Name" type="text" aria-describedby="emailHelp" placeholder="이름을 입력해주세요."><br>
                          <span class="nav-link-text">이메일(아이디)</span>
                          <input class="form-control" name="Email" type="email" aria-describedby="emailHelp" placeholder="이메일을 입력해주세요."><br>
                          <span class="nav-link-text">전화번호</span>
                          <input class="form-control" name="Tel" type="tel" aria-describedby="emailHelp" placeholder="ex)010-1234-5678">
                        </div>
                        <a id="getPassword" class="btn btn-primary btn-block" href="">비밀번호 찾기</a>
                     </form>
                  </div>
              </div>
          </div>
      </div>

      <script>
          $(document).ready(function() {

              $("#getEmail").on('click', function(e) {
                  var input = [ $("#id input[name=Name]").val(), $("#id input[name=Birth]").val(), $("#id input[name=Tel]").val() ];
                  e.preventDefault();
                  if(input[0] != '' && input[1] != '' && input[2] != '') {
                      alert('찾으시는 아이디는 abc@naver.com 입니다.');
                  } else {
                      alert('잘못된 입력정보 입니다.');
                  }
              });
              $("#getPassword").on('click', function(e) {
                  var input = [ $("#pwd input[name=Name]").val(), $("#pwd input[name=Email]").val(), $("#pwd input[name=Tel]").val() ];
                  e.preventDefault();
                  if(input[0] != '' && input[1] != '' && input[2] != '') {
                      alert('등록된 이메일로 비밀번호를 보냈습니다.');
                  } else {
                      alert('잘못된 입력정보 입니다.');
                  }
              });
          });
      </script>
  </body>
</html>
